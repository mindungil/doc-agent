# 행정 문서 자동 분류 시스템 상세 설계서

## 1. 개요

본 시스템은 행정 문서의 제목, 수신자 정보, 본문 내용을 분석하여 담당 부서를 자동으로 배정하거나, LLM을 통해 최적의 부서를 추천하는 하이브리드 검색 및 추론 시스템이다. 대규모 텍스트 처리의 효율성을 위해 DuckDB와 BM25S를 활용하며, 과거 이력과 사무분장 규정을 종합적으로 고려하는 증거 누적형(Snowball) 파이프라인을 채택한다.

---

## 2. 데이터 아키텍처

### 2.1 저장소 전략 (Storage Strategy)

데이터의 특성에 따라 정적 데이터(Cold)와 동적 데이터(Hot)를 분리하여 관리하며, 단일 진입점(View)을 통해 접근한다.

1. **사무분장 데이터 (Job Description)**
    - **저장소:** Qdrant (Vector DB)
    - **상태:** 구축되어 있음(6333포트: RESTFul API, 6334:gPRC), (부서, 담당 업무, 직급, 담당자 정보 포함), 컬렉션 명: dept_knowledege
2. **문서 배부 이력 데이터 (Document History)**
    - **저장소:** DuckDB + Parquet
    - **구조:**
        - **Cold Data (아카이브):** 월 단위 Parquet 파일 (예: `history_2024_01.parquet`). 압축률과 읽기 성능 최적화.
        - **Hot Data (실시간):** 당일 발생 데이터는 DuckDB 내부 테이블(`realtime_docs`)에 Append.
    - **통합 뷰 (Unified View):** DuckDB의 `VIEW` 기능을 사용하여 Parquet 파일들과 실시간 테이블을 하나의 가상 테이블로 통합 조회.
    
    → 구현해야함
    

### **2.2 검색 인덱스 전략 (Search Indexing) - Revised**

지속적 확장을 고려하여 BM25S 인덱스를 이원화하되, Main Index의 갱신 주기를 데이터 유입량에 따라 유연하게 가져가는 **Threshold-based Merge(임계값 기반 병합)** 방식을 채택한다.
**1. 인덱스 구조 (Index Structure)**
• **Main Index (Read-Optimized):** 대규모 과거 데이터를 포함한 정적 인덱스. (Parquet/DuckDB 기반)
• **Delta Index (Write-Optimized):** 아직 Main에 병합되지 않은 최신 데이터를 담는 메모리 상의 동적 인덱스.
**2. 운영 로직 (Life-cycle Management)**
**A. 실시간 처리 (Real-time Ingestion)**
• 새로운 문서가 유입되면 즉시 **Delta Index**에만 추가한다. (메모리 상에서 0.001초 소요)
• 검색 시: `Search(Main) + Search(Delta)` 결과를 합쳐서 반환하므로 사용자는 즉시 최신 글을 찾을 수 있다.
B. 병합 트리거 (Merge Trigger) - "언제 합칠 것인가?"
단순 배치가 아니라, 아래 세 가지 조건 중 하나라도 충족되면 백그라운드에서 병합(Re-indexing)을 실행한다.
1. **용량 임계값 초과 (Volume Threshold):**
    ◦ 조건: `Delta Index`의 문서 수가 **$N$건(예: 2,000건)**을 초과할 때.
    ◦ 이유: Delta가 너무 커지면 메모리를 많이 차지하고 검색 속도가 느려짐. 이때 Flush 해주는 것이 효율적.
2. **유휴 시간 활용 (Idle Time):**
    ◦ 조건: 마지막 병합 이후 **$T$시간(예: 6시간)**이 지났고, 시스템 부하가 적을 때.
    ◦ 이유: 데이터가 적게 들어오더라도 최소한의 최신화 주기 보장.
3. **관리자 강제 실행 (Manual/Bulk Trigger):**
    ◦ 조건: 대량의 과거 데이터(엑셀 1년 치 등)를 일괄 업로드했을 때.
    ◦ 이유: 대량 데이터는 Delta를 거치지 않고 즉시 Main으로 굽는 것이 빠름.
**3. 무중단 병합 프로세스 (Zero-downtime Merge)**
BM25S 인덱스를 다시 만드는 동안 검색이 멈추면 안 되므로, **Double Buffering** 기법을 사용한다.
1. **Snapshot:** 현재까지의 Delta 데이터를 Parquet(Cold Storage)에 Append 저장.
2. **Background Build:** 백그라운드 프로세스에서 `전체 Parquet 데이터`를 로딩하여 **새로운 Main Index(New)** 를 생성.
3. **Atomic Swap:** 생성이 완료되면, 검색 포인터를 `기존 Main`에서 `새로운 Main`으로 교체하고, `Delta`는 비운다(Reset).
    ◦ *교체 순간(밀리초 단위)에도 검색은 끊기지 않음.*

---

## 3. 처리 파이프라인 (Pipeline Specification)

### 3.1 전처리 및 정규화 (Preprocessing)

모든 입력 문서는 다음 과정을 거쳐 `Cleaned Title`로 변환되며, 이 값이 검색의 기준 키(Key)가 된다.

1. **패턴 검출 및 삭제:**
    - 정규식(Regex)을 사용하여 `( ... )` 및 `[ ... ]` 패턴을 추출한다.
    - **삭제 조건:** 괄호 내부 문자열 길이가 **3글자 이하**인 경우 무조건 삭제한다. (예: `(긴급)`, `[공지]` 등 제거)
    - **유지 조건:** 4글자 이상인 경우 유의미한 정보(사업명 등)로 판단하여 유지한다.
2. **공백 정규화:** 연속된 공백을 단일 공백으로 치환하고 앞뒤 공백을 제거한다.

### 3.2 수신자 필터링 (OCR & LLM Correction)

OCR로 추출된 수신자 텍스트를 LLM이 보정하여 구조화된 데이터로 변환한다.

- **입력:** OCR 추출 텍스트, 문서 원문 일부
- **LLM 작업:** 수신자가 명확한지, 참조/경유인지 판단.
- **출력 포맷 (JSON):**JSON
    
    `{
      "is_explicit": true,  // 단일 수신자가 명확하면 true, 다수거나 모호하면 false
      "dept_name": "행정정보과", // is_explicit가 true일 때만 값 할당 (예: 행정정보과장 -> 행정정보과)
      "reason": "수신란에 '행정정보과장'이 명시되어 있음"
    }`
    
- **로직:** `is_explicit`가 `true`인 경우, 이후의 검색 및 추론 단계에 제약 조건을 만들 수 있다.(행정정보과에서만 추출)

### 3.3 문자열 유사도 검증 및 후보군 추출 (Blocking & Similarity)

전수 비교의 시간 복잡도($O(N)$) 문제를 해결하기 위해 BM25S와 Edit Distance를 결합한다.

1. **1차 필터링 (Blocking):**
    - 도구: **BM25S (Main + Delta Index)**
    - 쿼리: `Cleaned Title` (Kiwi로 명사 추출 후 질의)
    - 동작: 전체 문서 중 키워드가 겹치는 상위 $K$개(예: 50개) 후보군 추출.
2. **2차 정밀 검증 (Scoring):**
    - 도구: **Levenshtein Distance** (RapidFuzz 활용)
    - 동작: 1차에서 추출된 50개 문서에 대해 문자열 유사도 계산.
3. **조기 종료 판단 (Threshold Check):**
    - 유사도가 **임계값 $T_{high}$ (예: 95%)** 이상인 과거 문서가 존재하는 경우:
        - 해당 과거 문서의 처리 부서로 **즉시 자동 배정**. (LLM 추론 생략)
    - 임계값 미만인 경우:
        - 다음 단계(증거 수집)로 진행.

### 3.4 증거 수집 (Evidence Accumulation)

조기 종료되지 않은 문서에 대해 LLM이 판단할 근거 데이터를 수집한다.

1. **Source A: 문서 배부 이력 (History)**
    - 위 3.3 단계에서 확보한 Top-$K$ 후보군 활용.
    - **수집 데이터:** `{유사 문서 제목, 처리 부서, 기안자(보고자), 기안 일자, 유사도 점수}`
    - **핵심:** 단순 부서명이 아닌 **'누가(Reporter)'** 기안했는지를 포함하여 인적 네트워크 추론의 근거로 삼는다.
2. **Source B: 사무분장 (Job Description)**
    - 도구: **Qdrant Vector Search**
    - 쿼리: `Cleaned Title` 벡터 임베딩
    - **수집 데이터:** `{담당 부서, 업무 정의 텍스트, 관련 직급/담당자, 유사도 점수}`

---

## 4. 의사결정 엔진 (LLM Reasoning)

수집된 증거들을 바탕으로 LLM이 최종 판단을 수행한다.

- **입력 프롬프트 구조:**
    - **Target:** 분석 대상 문서 정보 (제목, OCR 수신자 정보 등)
    - **Evidence 1 (History):** 과거 이력 Top-$K$ 리스트 (유사도 순)
    - **Evidence 2 (Job):** 사무분장 Top-$K$ 리스트
    - **Feedback Data:** (존재 시) 해당 보고자/키워드에 대한 과거 휴먼 피드백 이력 (후술)
- **추론 지시사항:**
    1. 과거 이력의 유사도와 기안자 패턴을 분석할 것.
    2. 사무분장의 규정상 업무 일치 여부를 확인할 것.
    3. 두 증거가 상충될 경우, 문서의 성격(단순 보고 vs 계약/지출 등)에 따라 행정 관례를 적용하여 판단할 것.
- **출력:** 최종 추천 부서(복수 가능), 선택 근거(Reasoning), 신뢰도(Confidence).

---

## 5. 휴먼 피드백 및 피드백 루프 (Feedback Loop)

최종 결정은 사람이 수행하며, 시스템은 사람의 수정 사항을 학습하여 2차 검증 로직에 반영한다.

### 5.1 검증 프로세스

1. **일괄 배정 전 검토:** 시스템은 예측 결과와 근거를 담당자에게 제시한다. (TIME_STAMP 기준 배부 실행 전 대기)
2. **담당자 확정/수정:** 담당자는 시스템의 추천을 승인하거나, 다른 부서로 수정한다.

→ 배부 설정 시간 이전에 시스템의 추천을 거부하지 않으면, 일괄 배부된다.

### 5.2 피드백 데이터베이스 (Feedback DB)

담당자가 LLM의 추천을 거부하고 수정한 경우, 해당 정보를 별도 저장한다.

- **테이블 구조:**
    - `keyword`: 문서 핵심 키워드 (또는 제목)
    - `reporter`: 문서의 기안자 (보고자)
    - `llm_predicted_dept`: LLM이 추천했던 부서 (오답)
    - `human_corrected_dept`: 사람이 수정한 부서 (정답)
    - `reason`: (선택) 수정 사유
    - `created_at`: 피드백 발생 일시

### 5.3 2차 검증 로직 (Inference Enhancement)

추후 새로운 문서가 유입될 때 LLM 추론 단계에서 다음 로직이 추가된다.

1. **피드백 조회:** 입력 문서의 `reporter` 또는 `keyword`가 Feedback DB에 존재하는지 확인.
2. **Context Injection:** 존재할 경우, LLM 프롬프트에 **"경고: 과거 이 기안자의 문서를 A부서로 예측했으나, 담당자가 B부서로 수정한 이력이 있음."** 이라는 정보를 명시적으로 주입.
3. **효과:** LLM이 동일한 실수를 반복하지 않고, 특정인의 문서는 특정 부서로 간다는 암묵지를 학습하게 됨.
