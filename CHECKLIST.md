# 시스템 점검 및 개선 사항

## 시스템 목표 복기

### 핵심 목표
전북도청 조직에서 외부/내부로부터 들어오는 문서를 자동으로 적절한 담당자에게 배부하는 POC 웹 서비스

### 주요 기능 요구사항
1. 문서 업로드 (PDF/DOCX/TXT)
2. 텍스트 추출
3. RAG 기반 벡터 검색 (Qdrant dept_knowledge 컬렉션)
4. LLM을 통한 담당자 추천 및 재순위화
5. 배부 확정 및 이력 관리
6. 통계 대시보드

### 기술 스택
- Backend: Python + FastAPI + uvicorn (포트 7000)
- Frontend: React + Vite + TypeScript + Tailwind CSS
- Vector DB: Qdrant (dept_knowledge 컬렉션)
- LLM: 외부 LLM API 연동
- Database: SQLite (문서 메타데이터)

---

## 현재 구현 상태

### 완료된 기능

#### Backend
- [x] 문서 업로드 API (`POST /api/documents/upload`)
- [x] 문서 목록 조회 API (`GET /api/documents/`)
- [x] 문서 상세 조회 API (`GET /api/documents/{id}`)
- [x] 담당자 추천 API (`POST /api/documents/{id}/recommend`)
- [x] 배부 확정 API (`POST /api/documents/{id}/assign`)
- [x] 오늘 통계 API (`GET /api/documents/stats/today`)
- [x] 텍스트 추출 서비스 (PDF/DOCX/TXT)
- [x] RAG 서비스 (Qdrant 벡터 검색)
- [x] LLM 서비스 (담당자 추천)
- [x] SQLite 데이터베이스 모델

#### Frontend
- [x] 대시보드 페이지 (통계 + 최근 문서)
- [x] 문서 목록 페이지
- [x] 문서 상세 페이지
- [x] 문서 업로드 컴포넌트 (드래그 앤 드롭)
- [x] 담당자 추천 결과 표시
- [x] 통계 배너 컴포넌트
- [x] React Query를 통한 데이터 페칭

#### 인프라
- [x] Docker Compose 구성
- [x] Backend Dockerfile
- [x] Frontend Dockerfile (Nginx)
- [x] 환경 변수 설정 (.env.example)

---

## 발견된 문제점 및 개선 사항

### 1. 자동 배부 플래그 처리 미흡
**문제**: `assign_document` API에서 `is_auto_assigned`가 항상 `False`로 설정됨
- 자동 추천 후 확정 시 `True`로 설정해야 함
- 수동 선택 시에만 `False`로 설정

**위치**: `backend/app/routers/documents.py:185`

**개선 방안**:
- `AssignmentRequest`에 `is_auto` 플래그 추가 또는
- 추천 결과에서 직접 확정하는 별도 엔드포인트 추가

### 2. 배부 이력 테이블 미구현
**문제**: 요구사항에 있던 "최근 문서 배부 이력 테이블"이 단순 최근 문서 목록으로만 표시됨
- 배부 일시, 자동/수동 여부, 담당자 정보를 테이블 형태로 표시 필요

**위치**: `frontend/src/pages/Dashboard.tsx`

**개선 방안**:
- 배부 완료된 문서만 필터링하여 테이블로 표시
- 컬럼: 문서 제목, 담당자, 부서, 배부 일시, 자동/수동 여부

### 3. 다중 파일 업로드 미지원
**문제**: 현재는 단일 파일만 업로드 가능
- 요구사항: "처음에는 단일 파일 업로드부터 시작 후, 다중 파일 확장 가능"

**개선 방안**:
- 현재는 단일 파일로 충분하지만, 향후 확장 고려
- 다중 파일 업로드 시 배치 처리 로직 필요

### 4. 에러 처리 개선 필요
**문제**: 
- LLM API 호출 실패 시 상세한 에러 메시지 부족
- Qdrant 연결 실패 시 처리 미흡
- 파일 업로드 실패 시 사용자 친화적 메시지 부족

**개선 방안**:
- 각 서비스 레이어에서 구체적인 예외 처리
- 프론트엔드에서 에러 상태 표시 개선

### 5. 로딩 상태 개선
**문제**: 
- RAG 분석 중, LLM 분석 중 상태는 있지만 UI 피드백 부족
- 문서 상세 페이지에서 추천 진행 상황 표시 필요

**개선 방안**:
- 진행률 표시 또는 단계별 상태 표시
- 스켈레톤 로딩 추가

### 6. 문서 요약 기능 미구현
**문제**: 요구사항에 "문서 요약 (LLM으로 자동 생성, 너무 무겁다면 나중에)" 언급
- 현재는 본문 미리보기만 제공

**개선 방안**:
- 선택적 기능으로 구현 (환경 변수로 활성화/비활성화)
- 문서 상세 조회 시 요약 생성 또는 캐싱

### 7. 담당자 검색/필터링 기능 부족
**문제**: 요구사항에 "사용자가 직접 담당자를 변경할 수 있는 드롭다운 or 검색 박스"
- 현재는 추천 후보 중에서만 선택 가능

**개선 방안**:
- 전체 직원 목록 검색 API 추가 (Qdrant에서 검색)
- 프론트엔드에 검색 입력 필드 추가

### 8. Docker Compose Qdrant 서비스 누락
**문제**: docker-compose.yml에 Qdrant 서비스가 없음
- 로컬 개발 시 Qdrant를 별도로 실행해야 함

**개선 방안**:
- docker-compose.yml에 Qdrant 서비스 추가 (선택적)

### 9. 환경 변수 검증 부족
**문제**: 필수 환경 변수 누락 시 런타임 에러 발생 가능

**개선 방안**:
- 애플리케이션 시작 시 필수 환경 변수 검증
- 명확한 에러 메시지 제공

### 10. API 문서화
**문제**: FastAPI 자동 문서화는 있지만, 사용 가이드 부족

**개선 방안**:
- README에 API 사용 예시 추가
- Swagger UI 접근 방법 안내

---

## 우선순위별 개선 계획

### 높은 우선순위 (POC 완성을 위해 필수)
1. ✅ **자동 배부 플래그 처리 수정** - 배부 확정 시 자동/수동 구분
2. ✅ **배부 이력 테이블 구현** - 대시보드에 배부 완료 문서 테이블 추가
3. ✅ **에러 처리 개선** - 사용자 친화적 에러 메시지

### 중간 우선순위 (사용성 개선)
4. ✅ **로딩 상태 개선** - 진행 상황 표시
5. ✅ **담당자 검색 기능** - 전체 직원 검색 기능 추가
6. ✅ **Docker Compose Qdrant 추가** - 로컬 개발 편의성
7. ✅ **환경 변수 검증** - 시작 시 검증 로직

### 낮은 우선순위 (향후 확장)
7. **다중 파일 업로드** - 배치 처리 기능
8. **문서 요약 기능** - LLM 기반 요약 생성
9. **환경 변수 검증** - 시작 시 검증 로직
10. **API 문서화 보완** - 사용 가이드 작성

---

## 테스트 필요 사항

### 기능 테스트
- [ ] 문서 업로드 (PDF/DOCX/TXT 각각)
- [ ] 텍스트 추출 정확도
- [ ] Qdrant 벡터 검색 동작
- [ ] LLM API 연동 및 응답 파싱
- [ ] 배부 확정 플로우
- [ ] 통계 계산 정확도

### 통합 테스트
- [ ] 전체 플로우: 업로드 → 추천 → 확정
- [ ] 에러 상황 처리 (Qdrant 연결 실패, LLM API 실패 등)
- [ ] 동시 요청 처리

### UI/UX 테스트
- [ ] 반응형 레이아웃 (모바일/태블릿/데스크톱)
- [ ] 로딩 상태 표시
- [ ] 에러 메시지 표시
- [ ] 접근성 (키보드 네비게이션 등)

---

## 다음 단계

1. **즉시 수정**: 자동 배부 플래그 처리 및 배부 이력 테이블 구현
2. **테스트 환경 구축**: Qdrant 및 LLM API 연결 테스트
3. **문서 보완**: 실행 가이드 및 트러블슈팅 가이드 작성
4. **성능 최적화**: 대용량 파일 처리, 벡터 검색 최적화

